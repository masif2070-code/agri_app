name: agri_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+2

environment:
  sdk: ^3.11.3

dependencies:
  firebase_core: ^4.5.0
  flutter:
    sdk: flutter
  flutter_map: ^8.2.2
  http: ^1.6.0
  latlong2: ^0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
